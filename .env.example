# npm run setup 自动生成 16 位登录密钥和独立的 JWT 签名密钥到 .env。
ADMIN_KEY=
ADMIN_JWT_SECRET=
# HTTPS 部署使用 true；仅本地 HTTP 调试设置为 false。
ADMIN_COOKIE_SECURE=true
# 反向代理部署可填写浏览器访问的完整源（不带末尾斜杠）。
# APP_ORIGIN=https://home.example.com
# DATABASE_PATH=/absolute/path/home.sqlite
